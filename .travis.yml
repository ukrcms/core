language: php
php:
  - 5.5
  - 5.4
  - 5.3

before_script:
  - composer self-update && composer --version
  - composer update --dev

script: phpunit --coverage-text --configuration dev/phpunit.xml dev/UcTests

notifications:
  email:
    - dev@funivan.com